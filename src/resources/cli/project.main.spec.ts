import { suite, test, slow, timeout, skip, only } from "mocha-typescript";
import * as assert from "assert";

@suite class {GAME_NAME}Spec {

     @test "Test name accessors"() {
        assert.equal(true, true);
     }
     
}